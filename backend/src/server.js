import'dotenv/config';import mongoose from'mongoose';import app from'./appCombined.js';const port=Number(process.env.PORT)||5000;if(!process.env.MONGODB_URI||!process.env.JWT_SECRET){console.error('MONGODB_URI and JWT_SECRET are required. Copy .env.example to .env.');process.exit(1)}mongoose.connect(process.env.MONGODB_URI).then(()=>app.listen(port,()=>console.log(`LUMA API listening on http://localhost:${port}`))).catch(err=>{console.error(`Database connection failed: ${err.message}`);process.exit(1)});
